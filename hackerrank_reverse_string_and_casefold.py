def rev(sentence):
    S1 = sentence.split()
    rev_list = S1[::-1]
    final_re = []
    final_words = ""
    swap_case_chars = ""
    print("rev st -:",rev_list)
    final_words = " ".join(rev_list)
    print("final words :",final_words)
    for words in f words:
            if char.isspace():
                print("hereisspace")
                " ".join(char)
            if char.isper():
                swap_case_chars += char.lower()
            elif char.islower()
                swap_case_chars += char.upper)
    print("swap case chars -:",swap_case_chars)


rev("aWesome iS cODING")
